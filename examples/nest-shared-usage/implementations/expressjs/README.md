# ExpressJS Example

This example demonstrates how to use `nest-shared` with ExpressJS.

## How to run

1. Install dependencies:

```bash
npm install
```

2. Build the project:

```bash
npm run build
```

3. Run the server:

```bash
npm start
```
